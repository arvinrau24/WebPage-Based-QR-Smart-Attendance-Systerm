# Vercel → Project → Settings → Environment Variables (then REDEPLOY)
# Must be your Railway Django URL — NOT your vercel.app URL. No /api suffix.
VITE_API_URL=https://your-railway-app.up.railway.app
